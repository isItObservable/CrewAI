apiVersion: v1
kind: Service
metadata:
  name: bmad-crew
  namespace: bmad-crew
  labels:
    app.kubernetes.io/name: bmad-crew
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bmad-crew
  ports:
    - name: http
      port: 80
      targetPort: http
